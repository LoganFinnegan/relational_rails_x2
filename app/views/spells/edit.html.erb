<%= form_with url: "/spells/#{@spell.id}", method: :patch do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :learned %>
  <%= f.text_field :learned %>

  <%= f.label :spell_level %>
  <%= f.number_field :spell_level %>

  <%= f.submit "Update Spell"%>
<% end %>